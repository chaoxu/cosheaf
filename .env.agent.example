# Agent-facing Cosheaf API config.
#
# Give agents this shape, not the server/operator .env.dev. The token is an
# opaque Cosheaf token from /api/v1/auth/login and is not valid at the backing
# forge.

COSHEAF_API_URL=https://cosheaf-test.lab/api/v1
COSHEAF_TOKEN=
