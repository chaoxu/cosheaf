@chaoxu:registry=http://100.93.22.80:3001/api/packages/chaoxu/npm/

# The editor package marks codemirror/lezer/katex/etc. as externals;
# Vite resolves them at the consumer's node_modules root, so they must be
# hoisted out of the editor's own node_modules.
shamefully-hoist=true
